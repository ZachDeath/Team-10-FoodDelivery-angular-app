export class menuOption {
    public id: number;
    public name: string;
    public photoPath: string;
    
    constructor(id: number, name: string, photoPath: string) {
        this.id = id;
        this.name = name;
        this.photoPath = photoPath;
    }
}